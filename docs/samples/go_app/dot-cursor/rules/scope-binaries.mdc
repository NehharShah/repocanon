---
description: Scope-specific guidance for binaries directories.
globs: cmd/**
alwaysApply: false
---

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate cursor .` to refresh inferred sections. -->

# scope-binaries

## Applies to

- `cmd/`

## Rule

Each subdirectory is a separate Go binary entry point. Shared code lives in `internal/` or `pkg/`, not in another binary's directory.
