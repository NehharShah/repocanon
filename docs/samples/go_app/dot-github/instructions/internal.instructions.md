---

applyTo: "internal/**"

---

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate copilot .` to refresh inferred sections. -->

## Go internal/ visibility (`internal/`)

Code under `internal/` is private to this module — Go enforces it at compile time. Treat it as private API: callers under `cmd/` and within `internal/` are fine, but do not export it via `pkg/` casually.
