---

applyTo: "__tests__/**"

---

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate copilot .` to refresh inferred sections. -->

## Test code in `__tests__/`

Match the assertion style and fixture patterns used by neighboring tests under `__tests__/`. New tests must be deterministic, not depend on external services, and not write outside the test's tmp directory.
