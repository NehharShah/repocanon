# @monorepo/web — AGENTS.md

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate agents .` to refresh inferred sections. -->

## Project overview

`@monorepo/web` is a monorepo repository written primarily in TypeScript. Detected frameworks/tools: Next.js, React, Vitest, Turborepo, TypeScript.

## Languages

- TypeScript (3 files)

## Frameworks and tools

- Next.js — web
- React — frontend
- Vitest — test
- Turborepo — monorepo
- TypeScript — language-tooling

## Package managers

- pnpm (`apps/web/package.json`)
- pnpm (`package.json`)
- pnpm (`packages/ui/package.json`)
- pnpm (`packages/utils/package.json`)

## Commands

- **Install**
  - `pnpm install`
- **Build**
  - `pnpm run build`
- **Test**
  - `pnpm run test`
- **Lint**
  - `pnpm run lint`

## Per-package commands

### `apps/web/` — @monorepo/web  (npm)
Frameworks: `Next.js`, `React`, `Vitest`

- **Install**
  - `npm install`
- **Dev**
  - `npm run dev`
- **Build**
  - `npm run build`
- **Test**
  - `npm run test`

### `packages/ui/` — @monorepo/ui  (npm)
Frameworks: `Vitest`, `TypeScript`

- **Install**
  - `npm install`
- **Build**
  - `npm run build`
- **Test**
  - `npm run test`

### `packages/utils/` — @monorepo/utils  (npm)
Frameworks: `TypeScript`

## Repository layout

- `apps/` — applications (monorepo)
- `packages/` — packages (monorepo)

## Conventions

- **JS/TS language**: TypeScript-first

## Architecture boundaries

- **package isolation** — Each package under `apps/`, `packages/` owns its own dependencies and should not reach into another package's internals.

## Preferred libraries

`turbo`, `typescript`.

## Uncertainty

- No LICENSE file detected — license intent is unclear.
- Low-confidence inference: test_layout — No test files detected.

## Validation expectations

Run lint, typecheck, and tests before claiming a change is complete. If any tool is undefined for this repo, say so explicitly rather than skipping it.

## Manual notes

<!-- repocanon:manual:begin -->

<!-- repocanon:manual:end -->
