---
description: High-level orientation for this repo.
alwaysApply: true
---

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate cursor .` to refresh inferred sections. -->

# project-overview

## What this repo is

`@monorepo/web` is a monorepo repository written primarily in TypeScript. Detected frameworks/tools: Next.js, React, Vitest, Turborepo, TypeScript.

## Languages

- TypeScript (3 files)

## Frameworks

- Next.js — web
- React — frontend
- Vitest — test
- Turborepo — monorepo
- TypeScript — language-tooling

## Package managers

- pnpm (`apps/web/package.json`)
- pnpm (`package.json`)
- pnpm (`packages/ui/package.json`)
- pnpm (`packages/utils/package.json`)

## Layout

- `apps/` — applications (monorepo)
- `packages/` — packages (monorepo)

## Preferred libraries

`turbo`, `typescript`.
