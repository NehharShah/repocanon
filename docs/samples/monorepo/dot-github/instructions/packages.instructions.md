---

applyTo: "packages/**"

---

<!-- Generated by RepoCanon. Safe to edit by hand; regenerate with `repocanon generate copilot .` to refresh inferred sections. -->

## Shared packages under `packages/`

Each package owns its own dependencies. Do not import from another package's internals; only consume the package's exported entry point.
